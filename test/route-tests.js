import test from 'ava';
import assert from 'assert';
import run, { UP, DOWN, ENTER } from 'inquirer-test';
const path = require('path');
const ROUTER = require('./../src/router');

/* start ################################### of java_dagger_defaut tests */

test("Given custom path", t => {
    
    // ROUTER.init([path.join(__dirname, "data/java_dagger_default")], undefined)
    t.fail("Write some tests please")
});