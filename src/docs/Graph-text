Component{
	Class name,
	Annotation scope, <- missing
	Module modules[],
	Class injections[]
}

Module{
	Class name,
	Annotation scope, <- missing
	Dependency[] providedDependencies
}

Dependency{
	Class name,
	Annotation scope, <- missing
	Class[] usages, <- missing
	Dependency[] dependency <- missing
}